# Dependências para rodar LOCALMENTE, na sua máquina Windows.
#
# Use assim, depois do requirements.txt:
#   python -m pip install -r requirements.txt
#   python -m pip install -r requirements-local.txt
#
# NÃO use este arquivo no Streamlit Cloud: o pacote MetaTrader5 é uma
# DLL do Windows e falha em servidor Linux.

MetaTrader5>=5.0.45

# --- Versões fixas por causa da CPU desta máquina ---
#
# O processador (Core2 Duo E8400, de 2008) não tem as instruções
# x86-64-v2 (SSE4.2/POPCNT) que os wheels de numpy >= 2.1 e
# pandas >= 2.2 para Windows exigem. Com versão incompatível, o import
# não dá erro Python: derruba o processo inteiro sem mensagem
# (código -1073741795 / 0xC000001D, "instrução ilegal").
#
# Estes pins vêm DEPOIS do requirements.txt de propósito: eles
# sobrescrevem o que foi instalado antes, corrigindo a versão só aqui.
numpy==1.26.4
pandas==2.1.4
